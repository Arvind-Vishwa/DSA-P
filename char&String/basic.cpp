/*
    creation char arr[10];
    vector<char>arr;
    >> sequence of character

    ASCII a-->97
    A ---> 65

    Formula of conversion ---> Uppercase to Lowercase
    ch = ch -'A' +'a';

    Formula of Lowecase conversion into the Uppercase
    ch = ch-'a' + 'A'

    creation of string
    string str;

    string name

    str[i] -->Access

    find --> if(name.find(word) != string::npos){ }  --> npos =-1

*/
